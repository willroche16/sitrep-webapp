import React from "react";
import ReactDOM from "react-dom/client";
import SitrepBoard from "./SitrepBoard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SitrepBoard />
  </React.StrictMode>
);
